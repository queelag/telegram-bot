{
  "author": "Dario Sechi",
  "devDependencies": {
    "@microsoft/api-extractor": "^7.33.5",
    "@queelag/core": "^1.1.12",
    "@queelag/telegram-bot-types": "^1.2.0",
    "@vitest/coverage-c8": "^0.24.5",
    "@vitest/ui": "^0.24.5",
    "dotenv": "^16.0.3",
    "fastify": "^4.9.2",
    "typescript": "^4.8.4",
    "vitest": "^0.24.5"
  },
  "license": "MIT",
  "main": "index.js",
  "name": "@queelag/telegram-bot",
  "peerDependencies": {
    "@queelag/core": ">= 1.1.12",
    "@queelag/telegram-bot-types": ">= 1.2.0"
  },
  "scripts": {
    "test": "yarn vitest run",
    "build": "rm -rf dist && yarn tsc && yarn api-extractor run --local",
    "tarball": "yarn version && cp LICENSE package.json README.md dist/. && cd dist && yarn pack",
    "deploy": "yarn test && yarn build && yarn tarball && yarn publish dist/*.tgz && git push"
  },
  "version": "1.9.1"
}
